<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menu_item_add_ticker"
        android:icon="@drawable/ic_menu_add"
        android:title="@string/add_ticker"
        app:showAsAction="ifRoom|withText"/>
    <item
        android:id="@+id/menu_item_delete_ticker"
        android:icon="@drawable/ic_menu_delete"
        android:title="@string/delete_ticker"
        app:showAsAction="ifRoom|withText"/>
    <item
        android:id="@+id/menu_item_refresh"
        android:icon="@drawable/ic_menu_refresh"
        android:title="@string/refresh_string"
        app:showAsAction="ifRoom|withText"/>
</menu>